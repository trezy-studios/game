// Spawn density represents the amount of entities that a spawn is allowed to have at any time
// The number is entities per 10 square blocks.
export const SPAWN_DENSITY = {
	VERY_LOW: 2,
	LOW: 5,
	MEDIUM: 10,
	HIGH: 25,
	VERY_HIGH: 50,
}

// Spawn rate times in milliseconds
export const SPAWN_RATE = {
	VERY_LOW: 300000,
	LOW: 60000,
	MEDIUM: 30000,
	HIGH: 15000,
	VERY_HIGH: 5000,
}
